export const PUBLIC_IMPORT_LIMITS = Object.freeze({
  maxFiles: 10,
  maxTextFileBytes: 2 * 1024 * 1024,
  maxImageFileBytes: 25 * 1024 * 1024,
  maxTotalBytes: 20 * 1024 * 1024,
});

export const PUBLIC_IMPORT_MAX_EXPANDED_SOURCE_LENGTH = 32 * 1024 * 1024;
export const PUBLIC_IMPORT_MAX_IMAGE_REPLACEMENTS = 8_192;
